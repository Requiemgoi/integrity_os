# Чеклист передачи проекта разработчику


## 📋 Быстрая команда для подготовки

```bash
# Windows (PowerShell)
# Удалить ненужные папки и файлы
Remove-Item -Recurse -Force backend\venv -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force frontend\node_modules -ErrorAction SilentlyContinue
Remove-Item -Force backend\*.db -ErrorAction SilentlyContinue
Get-ChildItem -Path backend -Recurse -Directory -Filter __pycache__ | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue

# Linux/Mac
# Удалить ненужные папки и файлы
rm -rf backend/venv
rm -rf frontend/node_modules
rm -f backend/*.db
find backend -type d -name __pycache__ -exec rm -r {} +
```

## 📦 Создание архива

```bash
# Windows
# Используйте WinRAR, 7-Zip или PowerShell
Compress-Archive -Path QarTech -DestinationPath QarTech_для_ИИ.zip

# Linux/Mac
tar -czf QarTech_для_ИИ.tar.gz QarTech
```

## 🔄 После получения проекта

```bash
# 1. Распаковать проект
# 2. Установить зависимости

# Backend
cd backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# Frontend
cd frontend
npm install

# 3. Запустить проект
# Backend: start_backend.bat/sh
# Frontend: start_frontend.bat/sh
```

