# Инструкция по запуску QarTech

## Шаг 1: Подготовка окружения

### Требования:
- **Python 3.8+** (проверьте: `python --version`)
- **Node.js 16+** (проверьте: `node --version`)
- **npm** (обычно устанавливается вместе с Node.js)

### Проверка установки:
```bash
python --version
node --version
npm --version
```

## Шаг 2: Запуск Backend (Серверной части)

### Вариант A: Использование готового скрипта (Windows)
1. Откройте командную строку (PowerShell или CMD)
2. Перейдите в папку проекта (замените путь на свой, если он отличается):
   ```bash
   cd C:\Projects\QarTech
   ```
3. Запустите скрипт:
   ```bash
   start_backend.bat
   ```

### Вариант B: Ручной запуск
1. Откройте терминал в папке проекта
2. Перейдите в папку backend:
   ```bash
   cd backend
   ```
3. Создайте виртуальное окружение:
   ```bash
   python -m venv venv
   ```
4. Активируйте виртуальное окружение:
   - **Windows:**
     ```bash
     venv\Scripts\activate
     ```
   - **Linux/Mac:**
     ```bash
     source venv/bin/activate
     ```
5. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
6. Запустите сервер:
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

### Что должно произойти:
- Вы увидите сообщение: `Application startup complete`
- Сервер будет доступен по адресу: `http://localhost:8000`
- API документация: `http://localhost:8000/docs`
- Будет создана база данных `qartech.db` в папке backend
- Автоматически создастся пользователь admin/admin123

**⚠️ НЕ ЗАКРЫВАЙТЕ это окно терминала!**

## Шаг 3: Запуск Frontend (Веб-интерфейса)

### Откройте НОВЫЙ терминал (не закрывая backend!)

### Вариант A: Использование готового скрипта (Windows)
1. В новом терминале перейдите в папку проекта (замените путь на свой, если он отличается):
   ```bash
   cd C:\Projects\QarTech
   ```
2. Запустите скрипт:
   ```bash
   start_frontend.bat
   ```

### Вариант B: Ручной запуск
1. В новом терминале перейдите в папку frontend:
   ```bash
   cd frontend
   ```
2. Установите зависимости (только при первом запуске):
   ```bash
   npm install
   ```
   Это может занять 2-5 минут при первом запуске.
3. Запустите приложение:
   ```bash
   npm start
   ```

### Что должно произойти:
- Браузер автоматически откроется на `http://localhost:3000`
- Если не открылся - откройте вручную: `http://localhost:3000`
- Вы увидите страницу входа (Login)

**⚠️ НЕ ЗАКРЫВАЙТЕ это окно терминала!**

## Шаг 4: Вход в систему

1. На странице входа введите:
   - **Username:** `admin`
   - **Password:** `admin123`
2. Нажмите "Sign In"
3. Вы будете перенаправлены на главную панель (Dashboard)

## Шаг 5: Использование приложения

### Главная панель (Dashboard)

#### Верхняя часть:
- **KPI карточки:** Показывают ключевые показатели (OEE, уровень запасов, скорость производства)
- **Кнопка Export:** Экспорт данных в CSV или PDF
- **Кнопка Refresh:** Обновление данных
- **Кнопка выхода:** Выход из системы

#### Центральная часть - Вкладки:
1. **Raw Materials (Сырье):**
   - Температура
   - Влажность
   - Количество
   - Вибрация

2. **Production Line (Производственная линия):**
   - Температура
   - Вибрация
   - Скорость производства
   - Процент брака (брак rate)
   - Давление

3. **Warehouse (Склад):**
   - Температура
   - Влажность
   - Уровень запасов
   - Вибрация

#### Правая панель:
- **Alerts (Оповещения):** Показывает активные предупреждения
  - Пороговые (threshold) - при превышении лимитов
  - ML-аномалии - обнаруженные алгоритмом

### Что происходит автоматически:
- ✅ Данные генерируются каждые 10 секунд
- ✅ Графики обновляются в реальном времени
- ✅ Оповещения появляются автоматически
- ✅ KPIs обновляются каждую минуту

## Шаг 6: Тестирование функций

### 1. Просмотр графиков
- Переключайтесь между вкладками
- Наблюдайте за обновлением графиков в реальном времени
- Каждый параметр имеет свой график

### 2. Просмотр оповещений
- Обратите внимание на правую панель
- Оповещения появляются при:
  - Превышении пороговых значений
  - Обнаружении аномалий ML-алгоритмом

### 3. Экспорт данных
- Нажмите кнопку "Export" в верхней панели
- Выберите:
  - **Export CSV** - для табличных данных
  - **Export PDF** - для отчета в PDF формате

### 4. Ручная генерация данных
- Можно использовать API для ручной генерации:
  - Откройте: `http://localhost:8000/docs`
  - Найдите endpoint: `POST /api/sensors/{sensor_type}/simulate`
  - Нажмите "Try it out" и выполните запрос

## Шаг 7: Остановка приложения

### Остановка Frontend:
- В терминале frontend нажмите `Ctrl + C`
- Подтвердите остановку

### Остановка Backend:
- В терминале backend нажмите `Ctrl + C`
- Подтвердите остановку

## Возможные проблемы и решения

### Проблема: Backend не запускается

**Ошибка: "Port 8000 is already in use"**
- Решение: Закройте другое приложение на порту 8000 или измените порт:
  ```bash
  uvicorn app.main:app --reload --host 0.0.0.0 --port 8001
  ```

**Ошибка: "Module not found"**
- Решение: Убедитесь, что виртуальное окружение активировано и зависимости установлены:
  ```bash
  pip install -r requirements.txt
  ```

**Ошибка: Python не найден**
- Решение: Установите Python 3.8+ с официального сайта python.org

### Проблема: Frontend не запускается

**Ошибка: "Port 3000 is already in use"**
- Решение: Закройте другое приложение на порту 3000 или измените порт:
  ```bash
  set PORT=3001
  npm start
  ```

**Ошибка: "npm install" не работает**
- Решение: 
  - Удалите папку `node_modules`
  - Удалите файл `package-lock.json`
  - Выполните снова: `npm install`

**Ошибка: "Cannot find module"**
- Решение: Переустановите зависимости:
  ```bash
  rm -rf node_modules
  npm install
  ```

### Проблема: Нет данных на дашборде

**Решение:**
1. Подождите 10-20 секунд (данные генерируются автоматически)
2. Проверьте, что backend запущен и доступен
3. Откройте консоль браузера (F12) и проверьте ошибки
4. Проверьте подключение WebSocket в консоли

### Проблема: WebSocket не подключается

**Решение:**
1. Убедитесь, что backend запущен
2. Проверьте в консоли браузера сообщения об ошибках
3. Проверьте URL в файле `frontend/src/services/websocketService.js`
4. Убедитесь, что CORS настроен правильно в backend

### Проблема: Ошибка авторизации

**Решение:**
1. Убедитесь, что используете правильные учетные данные:
   - Username: `admin`
   - Password: `admin123`
2. Проверьте, что backend запущен
3. Очистите кэш браузера и попробуйте снова

## Дополнительная информация

### API Документация
- Откройте в браузере: `http://localhost:8000/docs`
- Здесь вы можете:
  - Просмотреть все доступные endpoints
  - Протестировать API напрямую
  - Увидеть структуру запросов и ответов

### База данных
- Файл базы данных: `backend/qartech.db`
- Это SQLite база данных
- Можно открыть с помощью SQLite Browser или любого SQLite клиента

### Логи
- Backend логи выводятся в терминал
- Frontend логи можно увидеть в консоли браузера (F12)

## Следующие шаги для разработки

1. **Изучите код:**
   - Backend: `backend/app/`
   - Frontend: `frontend/src/`

2. **Настройте под свои нужды:**
   - Измените пороговые значения в `backend/app/services/alert_service.py`
   - Добавьте новые типы сенсоров в `backend/app/services/data_simulator.py`
   - Настройте внешний вид в `frontend/src/`

3. **Расширьте функционал:**
   - Добавьте новые типы сенсоров
   - Настройте дополнительные ML-модели
   - Добавьте новые графики и визуализации

## Поддержка

Если возникли проблемы:
1. Проверьте логи в терминале
2. Проверьте консоль браузера (F12)
3. Убедитесь, что все зависимости установлены
4. Проверьте версии Python и Node.js

---

**Успешной работы с QarTech! 🚀**

