# Что такое автоэнкодер и как он работает в QarTech?

## 🤖 Что такое автоэнкодер?

**Автоэнкодер (Autoencoder)** — это тип нейронной сети, которая учится сжимать и восстанавливать данные. Он состоит из двух частей:

1. **Энкодер (Encoder)** — сжимает входные данные в компактное представление (кодирование)
2. **Декодер (Decoder)** — восстанавливает данные из сжатого представления (декодирование)

### Простая аналогия:
Представьте, что вы хотите запомнить длинный текст. Вы:
- Сначала сжимаете его до ключевых слов (энкодер)
- Потом пытаетесь восстановить полный текст по этим ключевым словам (декодер)

Если вы хорошо запомнили, то восстановленный текст будет очень похож на оригинал. Если нет — будет много ошибок.

## 🎯 Как автоэнкодер используется для обнаружения аномалий?

### Принцип работы:

1. **Обучение на нормальных данных:**
   - Автоэнкодер обучается на исторических данных, которые считаются "нормальными"
   - Он учится хорошо восстанавливать эти нормальные паттерны

2. **Обнаружение аномалий:**
   - Когда приходят новые данные, автоэнкодер пытается их восстановить
   - Если данные "нормальные" (похожи на то, что он видел при обучении) → восстановление будет точным → **НЕ аномалия**
   - Если данные "аномальные" (не похожи на обученные) → восстановление будет неточным → **АНОМАЛИЯ!**

3. **Метрика ошибки:**
   - Измеряется разница между исходными данными и восстановленными
   - Большая ошибка = аномалия
   - Малая ошибка = норма

## 📊 Как это работает в QarTech?

### Процесс обучения:

1. **Сбор данных:**
   - Берется история за последние 168 часов (7 дней)
   - Данные группируются по сенсору и параметру (например, температура, вибрация)

2. **Создание окон (windows):**
   - Данные разбиваются на "окна" по 10 точек
   - Например: [t1, t2, t3, ..., t10], [t2, t3, t4, ..., t11], и т.д.
   - Это позволяет анализировать паттерны во времени

3. **Нормализация:**
   - Данные приводятся к диапазону 0-1 для лучшего обучения

4. **Обучение:**
   - 50 эпох (проходов по всем данным)
   - На каждой эпохе сеть учится лучше восстанавливать данные
   - Отслеживается "loss" (ошибка) — чем меньше, тем лучше

5. **Сохранение модели:**
   - Обученная модель сохраняется на диск
   - Может использоваться для обнаружения аномалий в реальном времени

### Процесс обнаружения аномалий:

1. Берется окно из 10 последних значений параметра
2. Автоэнкодер пытается восстановить это окно
3. Вычисляется ошибка восстановления
4. Если ошибка > порога (0.1) → **АНОМАЛИЯ**

## 📈 Что означают метрики обучения?

- **Эпохи (Epochs):** Количество полных проходов по всем данным (50)
- **Loss (Ошибка):** Мера того, насколько хорошо модель восстанавливает данные
  - Начальный loss: ошибка в начале обучения
  - Финальный loss: ошибка после обучения
  - Чем меньше loss, тем лучше модель выучила нормальные паттерны
- **Data Points:** Количество точек данных, использованных для обучения
- **Windows:** Количество окон, созданных из данных

## ✅ Как проверить, что обучение работает?

### В логах бэкенда вы увидите:
```
INFO: Starting autoencoder training for rm_vibration_001_vibration: 150 windows, 50 epochs
INFO: Epoch 1/50 - Loss: 0.123456
INFO: Epoch 10/50 - Loss: 0.098765
INFO: Epoch 20/50 - Loss: 0.087654
...
INFO: Training completed for rm_vibration_001_vibration. Final loss: 0.076543
```

### На фронтенде:
- Кнопка меняется на "🔄 Обучение..." во время обучения
- После завершения показываются детали:
  - Количество эпох
  - Изменение loss (начальный → финальный)
  - Количество данных и окон

### В консоли браузера:
```
✅ Обучение завершено для vibration: {
  epochs: 50,
  loss: "0.123456 → 0.076543",
  dataPoints: 168,
  windows: 150
}
```

## 🔍 Пример использования:

**Сценарий:** Обнаружение аномальной вибрации на станке

1. **Обучение:**
   - Собираются данные вибрации за неделю (нормальная работа)
   - Автоэнкодер учится восстанавливать нормальные паттерны вибрации

2. **Мониторинг:**
   - Каждый час берется окно из 10 последних значений
   - Автоэнкодер пытается восстановить это окно
   - Если вибрация нормальная → ошибка мала → всё ОК
   - Если вибрация аномальная (например, подшипник изношен) → ошибка большая → **АНОМАЛИЯ!**

3. **Результат:**
   - Система автоматически обнаруживает аномалии
   - Можно настроить алерты для уведомлений

## 💡 Преимущества автоэнкодера:

- ✅ Не требует размеченных данных (не нужно помечать, что аномально, а что нет)
- ✅ Учится на нормальных данных автоматически
- ✅ Может обнаруживать новые типы аномалий
- ✅ Работает с временными рядами (данными во времени)

## ⚠️ Важно знать:

- Обучение занимает время (обычно 10-30 секунд)
- Нужно достаточно данных для обучения (минимум 20 точек)
- Модель обучается для конкретного сенсора и параметра
- После обучения модель сохраняется и может использоваться сразу

---

**Теперь вы знаете, что происходит, когда вы нажимаете "Обучить автоэнкодер"!** 🎓

