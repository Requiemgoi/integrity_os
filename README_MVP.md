# IntegrityOS MVP (Hackathon Build)

## Как запустить

1. **Backend**: Запусти `start_backend.bat` (из корня проекта)
   - API будет доступно по адресу: http://localhost:8000
   - Документация API: http://localhost:8000/docs
   - Тестовый Админ: `admin` / `admin123` (создается автоматически при первом запуске)

2. **Frontend**: Запусти `start_frontend.bat` (из корня проекта)
   - Откроется в браузере: http://localhost:3000
   - Логин: `admin` / `admin123`

---

## Задачи команды (IntegrityOS)

Это "голый" каркас. Вся старая логика IoT вычищена. Нужно реализовать:

### 1. Давид (Backend / Database)
- **Файлы**: `backend/app/models.py`, `backend/app/routes/objects.py`
- **Что делать**:
  - БД: Таблицы `Pipeline` (трубопровод), `Object` (краны/компрессоры), `Inspection` (обследования), `Defect`.
  - API: Эндпоинты для загрузки CSV (`/import/objects`, `/import/inspections`).
  - API: CRUD для объектов и дефектов.

### 2. Арман (Frontend / Maps)
- **Файлы**: `frontend/src/pages/Dashboard.js`, `components/MapComponent.js`
- **Что делать**:
  - Заменить заглушку "Место для Карты" на реальную карту (Leaflet/React-Leaflet).
  - Отображать линии трубопроводов и точки дефектов.
  - Сделать таблицу со списком объектов.
  - Сделать форму загрузки файлов (Excel/CSV) -> отправлять на бэк Давида.

### 3. Нурым (AI / Gemini / Analytics)
- **Файлы**: `backend/app/services/gemini_service.py`, `routes/ai.py`
- **Что делать**:
  - Сервис для обращения к Google Gemini API.
  - Логика оценки критичности дефекта (Rule-based + AI подтверждение).
  - Дашборд: графики рисков и статистика по дефектам.

---

## Структура проекта

- `backend/` - FastAPI (Python)
  - `app/main.py` - точка входа (чистая)
  - `app/models.py` - модели БД (нужно обновить)
  - `app/routes/` - роуты API (нужно добавить свои)
- `frontend/` - React (JS)
  - `src/pages/Dashboard.js` - главная страница (сейчас заглушка)
  - `src/components/` - компоненты UI

Удачи на хакатоне! 🚀
