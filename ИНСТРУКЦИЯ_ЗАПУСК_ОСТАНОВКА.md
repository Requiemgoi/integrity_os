# Инструкция по запуску и остановке QarTech

## 🛑 Остановка приложения

### Шаг 1: Остановить Frontend
В терминале, где запущен frontend:
1. Нажмите `Ctrl + C`
2. Подтвердите остановку (если спросит)
3. Терминал можно закрыть

### Шаг 2: Остановить Backend
В терминале, где запущен backend:
1. Нажмите `Ctrl + C`
2. Подтвердите остановку (если спросит)
3. Терминал можно закрыть

**Готово!** Приложение остановлено.

---

## ▶️ Запуск приложения (после остановки)

### Вариант 1: Использовать готовые скрипты (Самый простой)

#### Windows:
1. Откройте **CMD** (командную строку)
2. Перейдите в папку проекта:
   ```cmd
   cd G:\College\Project\QarTech
   ```
3. Запустите backend (в первом терминале):
   ```cmd
   start_backend.bat
   ```
4. Откройте **новый терминал CMD** и запустите frontend:
   ```cmd
   cd G:\College\Project\QarTech
   start_frontend.bat
   ```

#### Linux/Mac:
1. Откройте терминал
2. Перейдите в папку проекта:
   ```bash
   cd /path/to/QarTech
   ```
3. Запустите backend:
   ```bash
   ./start_backend.sh
   ```
4. Откройте **новый терминал** и запустите frontend:
   ```bash
   cd /path/to/QarTech
   ./start_frontend.sh
   ```

---

### Вариант 2: Ручной запуск

#### Backend (первый терминал):
```cmd
cd G:\College\Project\QarTech\backend
venv\Scripts\activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### Frontend (второй терминал):
```cmd
cd G:\College\Project\QarTech\frontend
npm start
```

---

## 📋 Быстрая шпаргалка

### Остановка:
- Backend: `Ctrl + C` в терминале backend
- Frontend: `Ctrl + C` в терминале frontend

### Запуск:
- Backend: `start_backend.bat` (Windows) или `./start_backend.sh` (Linux/Mac)
- Frontend: `start_frontend.bat` (Windows) или `./start_frontend.sh` (Linux/Mac)

---

## ⚠️ Важно

1. **Всегда запускайте backend ПЕРВЫМ** - frontend зависит от него
2. **Используйте два разных терминала** - один для backend, другой для frontend
3. **Не закрывайте терминалы** пока работаете с приложением
4. **Backend должен быть запущен** перед запуском frontend

---

## 🔄 Порядок запуска

1. ✅ Запустить backend → дождаться "Application startup complete"
2. ✅ Запустить frontend → дождаться открытия браузера
3. ✅ Войти: `admin` / `admin123`

---

## 🚀 Быстрая команда для Windows

Откройте два терминала CMD:

**Терминал 1 (Backend):**
```cmd
cd G:\College\Project\QarTech && start_backend.bat
```

**Терминал 2 (Frontend):**
```cmd
cd G:\College\Project\QarTech && start_frontend.bat
```

---

## 📝 Проверка работы

После запуска проверьте:
- Backend: http://localhost:8000 (должен показать JSON)
- API Docs: http://localhost:8000/docs (должна открыться документация)
- Frontend: http://localhost:3000 (должен открыться интерфейс входа)

---

**Готово!** Теперь вы знаете, как останавливать и запускать приложение.

