# Задание для разработчика ИИ - QarTech Project

## 📋 Задача

Реализовать ИИ для генерации графиков, визуализаций и аналитики на странице дашборда проекта QarTech.

## 🎯 Что нужно сделать

### 1. Реализовать функцию `renderAIGeneratedContent()` в `DashboardTab.js`

**Файл:** `frontend/src/components/DashboardTab.js`

**Задача:** Генерировать визуализации и анализ данных сенсоров.

**Доступные данные:**
- `sensorData` - массив всех точек данных за последние 24 часа
- `latestData` - массив последних значений для каждого параметра
- `dataByParameter` - объект с данными, сгруппированными по параметрам
- `parameters` - массив уникальных параметров (temperature, humidity, vibration, etc.)
- `summary` - сводная информация о сенсорах
- `sensorType` - тип сенсора ('raw_material', 'production_line', 'warehouse')

**Что генерировать:**
- Графики для каждого параметра
- Диаграммы для анализа данных
- Индикаторы текущих значений
- Аналитические инсайты
- Рекомендации на основе данных
- Предупреждения и алерты

**Пример структуры данных:**
```javascript
// sensorData
[
  {
    id: 1,
    sensor_id: "sensor_001",
    sensor_type: "raw_material",
    parameter: "temperature",
    value: 20.5,
    unit: "°C",
    timestamp: "2024-01-01T12:00:00",
    is_anomaly: false
  },
  // ...
]

// dataByParameter
{
  'temperature': [/* массив точек данных */],
  'humidity': [/* массив точек данных */],
  // ...
}

// summary
{
  sensor_count: 4,
  recent_data_points: 120,
  active_alerts: 2
}
```

### 2. Реализовать функцию `renderAIGeneratedKPIs()` в `KPICards.js`

**Файл:** `frontend/src/components/KPICards.js`

**Задача:** Генерировать визуализации ключевых показателей эффективности (KPI).

**Доступные данные:**
- `kpis` - массив объектов KPI

**Структура данных KPI:**
```javascript
[
  {
    id: 1,
    kpi_name: "OEE",              // 'OEE', 'stock_level', 'production_rate'
    value: 85.5,                   // Текущее значение
    target: 90.0,                  // Целевое значение (опционально)
    unit: "%",                     // Единица измерения
    timestamp: "2024-01-01T12:00:00"
  },
  // ...
]
```

**Что генерировать:**
- Графики для каждого KPI
- Индикаторы прогресса к целевым значениям
- Сравнение с целевыми значениями
- Анализ трендов
- Рекомендации по улучшению показателей

## 🛠️ Технические требования

### Библиотеки для графиков

В проекте уже установлен **Chart.js**. Можно использовать:
- Chart.js (уже установлен)
- react-chartjs-2 (уже установлен)
- Другие библиотеки (можно добавить)

### Реализация

Вы можете реализовать ИИ одним из способов:

1. **Как компонент** - создать новый React компонент
2. **Как сервис** - создать сервис для работы с ИИ
3. **Как хук** - создать React хук
4. **Прямо в функции** - реализовать логику прямо в функции

### Real-time обновления

**Важно:** Данные обновляются автоматически через WebSocket каждые 10 секунд. ИИ должен реагировать на обновления и автоматически обновлять визуализации.

## 📚 Документация

### Основная документация
- `ИИ_ИНТЕГРАЦИЯ.md` - подробная документация о данных и интеграции
- `API_DOCUMENTATION_SWAGGER.md` - документация API
- `АРХИТЕКТУРА_ПРОЕКТА.md` - архитектура проекта

### Сервисы для работы с данными

#### sensorService
```javascript
import { sensorService } from '../services/sensorService';

// Получить данные сенсоров
const data = await sensorService.getSensorData(sensorType, hours, limit);

// Получить последние данные
const latest = await sensorService.getLatestSensorData(sensorType);
```

#### dashboardService
```javascript
import { dashboardService } from '../services/dashboardService';

// Получить KPI
const kpis = await dashboardService.getKPIs();

// Получить алерты
const alerts = await dashboardService.getAlerts(limit);

// Получить сводку
const summary = await dashboardService.getSummary();
```

#### websocketService
```javascript
import { websocketService } from '../services/websocketService';

// WebSocket уже подключен в компонентах
// Данные обновляются автоматически
```

## ⚠️ Важные замечания

### Что НЕЛЬЗЯ изменять

- ❌ `backend/app/main.py` - главный файл приложения
- ❌ `backend/app/routes/*.py` - маршруты API
- ❌ `frontend/src/pages/Dashboard.js` - главная страница
- ❌ `frontend/src/services/*.js` - существующие сервисы (можно добавлять новые)
- ❌ Логику загрузки данных - она уже работает

### Что можно изменять

- ✅ `frontend/src/components/DashboardTab.js` - только функция `renderAIGeneratedContent()`
- ✅ `frontend/src/components/KPICards.js` - только функция `renderAIGeneratedKPIs()`
- ✅ Создавать новые компоненты для ИИ
- ✅ Создавать новые сервисы для ИИ
- ✅ Создавать новые хуки для ИИ
- ✅ Добавлять новые зависимости в `package.json`

## 🚀 Запуск проекта

### Backend
```bash
# Windows
start_backend.bat

# Linux/Mac
./start_backend.sh
```

Backend будет доступен на: http://localhost:8000

### Frontend
```bash
# Windows
start_frontend.bat

# Linux/Mac
./start_frontend.sh
```

Frontend будет доступен на: http://localhost:3000

### Вход в систему
- **Username:** `admin`
- **Password:** `admin123`

## 📊 API документация

API документация доступна на: http://localhost:8000/docs

Там вы можете:
- Просмотреть все доступные endpoints
- Протестировать API
- Посмотреть структуру данных

## 🧪 Тестирование

### Проверка данных

1. Откройте дашборд: http://localhost:3000
2. Войдите в систему
3. Проверьте, что данные загружаются
4. Переключайтесь между вкладками (Сырье, Производственная линия, Склад)
5. Проверьте, что данные обновляются в реальном времени

### Проверка ИИ

1. Убедитесь, что графики отображаются
2. Убедитесь, что KPI визуализируются
3. Убедитесь, что анализ работает
4. Убедитесь, что рекомендации генерируются
5. Убедитесь, что real-time обновления работают

## 📝 Примеры кода

### Пример 1: Простой график с Chart.js

```javascript
import { Line } from 'react-chartjs-2';

const renderAIGeneratedContent = () => {
  // Подготовка данных для графика
  const chartData = {
    labels: sensorData.map(point => new Date(point.timestamp).toLocaleTimeString()),
    datasets: [
      {
        label: 'Temperature',
        data: dataByParameter['temperature']?.map(point => point.value) || [],
        borderColor: 'rgb(75, 192, 192)',
        backgroundColor: 'rgba(75, 192, 192, 0.2)',
      },
    ],
  };

  return (
    <Grid container spacing={3}>
      {parameters.map((param) => (
        <Grid item xs={12} md={6} key={param}>
          <Paper sx={{ p: 2 }}>
            <Typography variant="h6">{param}</Typography>
            <Line data={chartData} />
          </Paper>
        </Grid>
      ))}
    </Grid>
  );
};
```

### Пример 2: Визуализация KPI

```javascript
import { Doughnut } from 'react-chartjs-2';

const renderAIGeneratedKPIs = () => {
  return (
    <Grid container spacing={2}>
      {kpis.map((kpi) => {
        const progress = kpi.target ? (kpi.value / kpi.target) * 100 : 0;
        
        const chartData = {
          datasets: [
            {
              data: [progress, 100 - progress],
              backgroundColor: ['rgb(76, 175, 80)', 'rgba(0, 0, 0, 0.1)'],
            },
          ],
        };

        return (
          <Grid item xs={12} sm={6} md={3} key={kpi.id}>
            <Card>
              <CardContent>
                <Typography variant="h6">{kpi.kpi_name}</Typography>
                <Typography variant="h4">{kpi.value} {kpi.unit}</Typography>
                {kpi.target && (
                  <Doughnut data={chartData} />
                )}
              </CardContent>
            </Card>
          </Grid>
        );
      })}
    </Grid>
  );
};
```

## ✅ Чеклист выполнения

- [ ] Реализована функция `renderAIGeneratedContent()` в `DashboardTab.js`
- [ ] Реализована функция `renderAIGeneratedKPIs()` в `KPICards.js`
- [ ] Графики отображаются для всех параметров
- [ ] KPI визуализируются корректно
- [ ] Аналитические инсайты отображаются
- [ ] Рекомендации генерируются
- [ ] Real-time обновления работают
- [ ] Код работает без ошибок
- [ ] Производительность приемлемая
- [ ] Ошибки обрабатываются корректно

## 📞 Вопросы?

Если у вас возникли вопросы:
1. Проверьте документацию в проекте
2. Проверьте API документацию: http://localhost:8000/docs
3. Проверьте код компонентов (есть комментарии)
4. Свяжитесь с заказчиком для уточнений

---

Удачи в разработке! 🚀

